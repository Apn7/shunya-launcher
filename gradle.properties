# Gradle daemon memory; UTF-8 so Bangla strings and sources compile identically on Windows.
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
# AndroidX packages only.
android.useAndroidX=true
# Kotlin code style for IDEs and formatters.
kotlin.code.style=official
